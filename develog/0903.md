# defragmentation 최소화를 위해서
defragmentation때문에 큰애들을 할당할때 위험부담이 제법 생김 (특히 기능 설정쪽) 여러가지 해결책이 있는데
- jpeg 큰놈을 짠짠바리로 바꾸기 [OK]
- canvas 드시고계신거 다 없애버리기 [OK]
- 기능 설정 -> 고장진단을 prewarm 생명주기에서 빼고 하기 전에는 아예 drop해서 엄청 느리지만 안전하게 (최후의 옵션) [NO]
- ram partition을 나누어서 아예 따로 관리 ^2 [NO]
- image buffer의 크기는 알고있으니까, buffer 할당을 램 파티션의 뒤쪽에서부터 오게 하기 [TODO!]
- round robin 방식의 init queue를 FIFO로 바꿔서 페이지 내부의 구성요소들이 파편화 되는것을 방지 (짠짠바리 에셋들) [OK]

# 최적화를 위해서
- 페이지 init + cb 다 돌아보면서 init이랑 loaded 확인해서 lazy create를 다 제거해야함 [TODO!]
- 페이지 init 다 돌아보면서 deinit에서 이미지 확실하게 반납시키고 내부에서 쓴 포인터 전부다 NULL로 [TODO!]
- 페이지별 살려둬야할 생명페이지들 최적화시키기 [TODO!]

# 기능 버그
- 정전 복구용 기능들 새로운 관리자에 안맞음 (main_activity_init) [OK]
- lang, 화씨 관련 기능들 새로운 관리자에 안맞음 [TODO!]

---
^1
(auto mode end는 에셋 자체가 없어서 불가능)
^2
(backup/ram-allocator 브랜치 확인 : PSRAM이 아예 여유가 없어서 decoder가 사용하는 버퍼 + 이미지 버퍼 두개를 공용하지 않으면 자리가 모자람. 디코더를 이미지버퍼에 땡겨오니 lvgl에서 썼다 말았다 하면서 auth없는 free하니까 크래쉬남. 일단 지금은 이 방식은 못 건드리겠음.)