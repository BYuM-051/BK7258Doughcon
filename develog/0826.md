UI Init 구조 점검 Worknote
대상 브랜치: latency-optimize
기준 시작 커밋: 3533e3ef65f719c12407d65fb693730697372649
최신 확인 커밋: 078b86344feee095db80386e441968d6d99b8d0c
조사 경로: projects/lvgl/korea_test/ap/ui
폰트 소스 제외
아래 진행 상태는 커밋 메시지만 기준으로 갱신
코드 내용 및 실제 동작 검증은 아직 수행하지 않음
오늘 반영된 커밋
bb625ac — fix) rootScreen multiple definition problem
abc7f0b — fix) main_bg multiple create problem
4903a58 — mod) 15screen : event callback
71b5d8c — fix) page lifecycle manager bug
1b1dfec — fix) match with new lifecycle manager
96d93d2 — add) print returning addr ui screen change to null conition
3eb6d17 — add) page lifecycle manager lvgl_scr selectable structure
e4d8e39 — fix) setting mode init callback update
7f31083 — try) -- have to review!! -- autonomous implements for new lifecycle manager callback
078b863 — fix) match with new lifecycle manager : mainPage
진행 현황
1. preRenderRoot 중복 정의
 수정 커밋 반영
관련 커밋: bb625ac
커밋 메시지상 root screen의 multiple definition 문제가 수정됨.
실제 정의·extern 배치와 링크 결과는 후속 검증 필요.
2. Pre-render 페이지 생명주기 전환
[~] 구현 진행 중
관련 커밋:
4903a58 — 15개 화면 이벤트 콜백 변경
71b5d8c — 페이지 생명주기 관리자 버그 수정
1b1dfec — 새 생명주기 관리자 대응
96d93d2 — 화면 전환 대상이 NULL일 때 호출자 주소 출력 추가
3eb6d17 — LVGL screen을 선택할 수 있는 생명주기 관리자 구조 추가
e4d8e39 — Setting Mode init callback 갱신
7f31083 — 새 생명주기 관리자 callback 자동 이관 시도
078b863 — Main Page의 새 생명주기 관리자 대응 수정
15개 화면의 이벤트 콜백 이관 작업이 반영된 것으로 기록됨.
mainPage와 settingMode 관련 후속 수정도 반영됨.
다만 7f31083 커밋 메시지에 명시적으로 have to review가 포함되어 있으므로 완료로 판정하지 않음.
각 화면의 이벤트 매핑과 화면 전환 호출은 코드 리뷰 및 실행 검증 필요.
3. main_bg 중복 생성
 수정 커밋 반영
관련 커밋: abc7f0b
커밋 메시지상 main_bg 중복 생성 문제가 수정됨.
실제 객체 생성 횟수와 전처리기 분기는 후속 검증 필요.
4. Boot warmup 위치 및 동작
 기존 위치 및 문제 확인 완료
 수정 여부 미확인
오늘 커밋 중 Boot warmup을 직접 명시한 커밋은 없음.
기존 분석 결과를 유지함.
생명주기 관리자 변경이 warmup 동작에 간접적인 영향을 줬는지는 코드 리뷰 후 재판정 필요.
5. Static LVGL style 반복 초기화
 문제 확인 완료
 수정 커밋 없음
init_keypad_group()의 static style 반복 초기화 문제를 직접 언급한 커밋은 없음.
기존 미수정 상태로 기록함.
6. Emulator 경로 통합
 구현 여부 미확인
3eb6d17에서 LVGL screen 선택 구조가 추가됐지만, 커밋 메시지만으로 emulator 통합 여부를 확정할 수 없음.
emulator 또는 HAL_USE_EMULATOR를 직접 언급한 커밋은 없음.
전체 생명주기 전환이 끝난 뒤 별도 구현 및 실행 검증 필요.
7. 페이지별 초기화 비용 분석
 상세 분석 미진행
오늘 커밋 중 페이지별 초기화 비용, 객체 수, 이미지 decode, canvas allocation 또는 latency 측정을 직접 언급한 커밋은 없음.
생명주기 전환 코드가 안정화된 뒤 다음 항목을 분석해야 함.
init 시 즉시 생성되는 객체
이벤트 진입 후 지연 생성되는 객체
이미지 decode 및 canvas allocation
timer 및 animation 생성
destroy/recreate와 keep-alive 여부
최초 진입과 재진입 latency
실제 최적화 우선순위
현재 결론
항목	상태	커밋 메시지 기준 판정
1. preRenderRoot 중복 정의	수정 반영	후속 검증 필요
2. 페이지 생명주기 전환	진행 중	자동 이관 결과 리뷰 필요
3. main_bg 중복 생성	수정 반영	후속 검증 필요
4. Boot warmup	기존 조사 완료	관련 수정 커밋 없음
5. Static style	문제 확인	관련 수정 커밋 없음
6. Emulator 통합	보류	명시적인 관련 커밋 없음
7. 초기화 비용 분석	보류	관련 분석 커밋 없음