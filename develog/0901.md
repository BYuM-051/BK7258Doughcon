버그목록
메모리모드 진입시 lazy-init 객체로 인한 느린 진입.
한국어가 아닐 경우 이상하게 첫 진입이 느림.
한국어가 아닐 경우 메모리모드 assert됨.
섭씨 화씨 변경시 assert됨.

구현필요
페이지별로 step_init + deinit + callback수정
페이지를 queue단위로 관리하는 관리자 -> ok
prewarm이 되고도 연결안된 decode image buffer free -> pointer NULL 확인 후 drop하면 될듯? -> ok