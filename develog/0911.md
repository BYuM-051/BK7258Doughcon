# 금요일 디벨로그. 오늘 출하예정
---
## 발견된 버그 (꾸준히 업데이트)
1. 정전복구 메커니즘에 큰 문제가 있음. 
    1. 정전시 냉동/해동 -> 완료시간 동일에 남은시간이 감소
    2. 정전시 저온발효/고온발효 -> 완료시간 증가에 남은시간이 유지
    3. 해동 정전시 -> 복귀 해동X 저온발효 에러남
    4. 저온 정전시 -> 고온 발효로 넘어감
    5. 고온 정전시 -> ?
---
## 해결된 버그 (오늘까지)
- ~~패스워드 팝업 이상~~
    - setting preheat 대상에 추가해서 핫픽스.
- ~~자동건조 링이 늦게 표시됨~~
    - event의 lazy create를 refresh 전에 실행하도록 변경.
- ~~메모리 모드 진입 시 언어 적용 및 박스 표시가 늦음~~
    - lazy create를 refresh 전에 실행하고 `change_page_showOption` 설정 수정.
- ~~테스트 모드 박스 언어 적용 안 됨 / JPG 참조~~
    - 기존 불필요 버퍼 제거.
    - 잘못된 JPG 참조 제거.
    - lazy create 이후 refresh하도록 변경.
- ~~수동디테일 언어 적용 안 됨 / 중간 글씨 늦게 표시됨~~
    - `LOADED`에서 갱신 후 refresh하도록 변경.
    - `showOption false` 적용.
- ~~automodestart 구형 background 로직 잔존~~
    - 구형 background cache 로직 제거.
- ~~language setting 배경 적용 안 됨~~
    - 기존 background update 함수 호출하도록 수정.
    - 언어 리셋 시 predecode 재스케줄링 버그 수정.
- ~~temperature unit setting 버그~~
    - 잘못된 deinit 처리 수정.
- ~~비프음 울리는 시기가 제각각~~
    - 비프 실행 시점을 앞으로 통일.
- ~~자동설정에서 memorymode 진입 시 `showOption false` 필요~~
    - `showOption false` 적용.
- ~~로그가 너무 많고 이미지 dump가 불필요함~~
    - 이미지 dump 로그 비활성화.
- ~~수동모드 작동 시 화면 갱신 안 됨~~
    - `lv_scr_act()` 대신 `ui_get_current_page()` 적용.
    - 시간만 떨어지는 경로가 생기지 않도록 수정.
    - `keypad_hide`가 hidden이 아니던 문제 수정.
- ~~automode style init이 page create 외에도 실행됨~~
    - style init을 page create 시점에서만 실행하도록 변경.
- ~~screen off 시 hard fault~~
    - 기본 thread stack 문제를 피하기 위해 LVGL 작업을 async로 넘기도록 변경.
    - 새로운 생명주기에서 main -> main 전환 시 fault가 나지 않도록 guard 추가.
    - screen off 시 터치 비활성화 추가.
- ~~룸팬 asset 색상 불일치~~
    - Android 기본 버전 asset으로 롤백.
- ~~디테일 -> 뉴로시스 코드 화면 터치 불가~~
    - `preRenderRoot`를 직접 참조하도록 변경.
- ~~저온발효모드 전환스크린 background 깨짐~~
    - tree 구조에 맞지 않는 구형 canvas buffer 사용 코드 제거.
    - background object에 PNG file buffer를 사용하도록 변경.
    - JPEG은 현재 evictable LVGL object buffer에서 지원하지 않아 PNG 유지.
- ~~automode invalid date일 때 keypad 버튼 외 UI 조작 가능~~
    - invalid date 상태에서는 keypad 버튼 외 UI를 조작할 수 없도록 변경.
- ~~keypad 표시 중 주변 UI 조작 가능~~
    - keypad 표시 중 주변 object를 clickable하지 않도록 변경.
    - autodry 적용.
    - automode 적용.
    - manualmodestart 적용.
    - settingmodetime 적용.
- ~~asset update 이후 시간설정 keypad 버튼이 표시되지 않음~~
    - keypad asset 누락 문제 수정.
    - autodry 적용.
    - settingmodetime 적용.
- ~~keypad 3종의 beep 동작이 원본과 다름~~
    - mute 여부와 관계없이 필요한 beep가 동작하도록 수정.
    - screen off toggle에서 mute mode가 아닐 때 beep가 나지 않던 조건 수정.
- ~~PNG asset 중 type-2 파일 사용~~
    - type-2 PNG를 type-3 PNG로 변경.
    - 파일 크기와 decode 시간 감소.
- ~~realloc 실패 시 buffer를 비우고 다시 realloc할 필요가 있음~~
    - 기존 코드에 이미 해당 처리 존재 확인.
- ~~automodeend 구형 background 로직 확인 필요~~
    - 해당 구형 background 로직이 존재하지 않는 것 확인.
- ~~automode 정규화 알고리즘 잘못됨 확인~~
    - 입력 year clamp가 2024년으로 하나만 잘 못 걸려있는것을 발견. 나머지 클램프들은 2020년이므로 실제 날짜가 2020년으로 설정됐을 때 soft-lock 상황임
    - 클램프들이 걸려있는 곳을 일괄 RTC기준으로 변경할 필요가 있음 <TBD>
    - 우선은 2024년 clamp를 2020년으로 변경해서 해결
- ~~language select 변경"후" deinit이라 managed deinit을 적용한다면 img cache가 계속 누수됨 > language select 최적화시에 신경쓸필요~~
    - 새로운 deinit 관리자 만들었음. ㄱㅊ.
- ~~부팅시에 trash image 나오지 않도록~~
    - splash init 후 RGB 병렬 여유시간 주고 bl on
- ~~외부 keypad reboot시에 재부팅이 너무 빨라서 비프음이 끊김~~
    - 100ms만큼 기다려 해결
- ~~timebar가 main보다 먼저 splash 바깥으로 던져지는 문제~~
    - init에서 적절히 조작하여 해결
- ~~터치 queue에 명령이 누적됨~~
    - ~~page change 시 필요한 일부만 남기고 touch queue를 flush하도록 변경.~~
    - ~~language 직접전환 경로에도 동일 처리.~~
    - ~~temperature unit 직접전환 경로에도 동일 처리.~~
        - touch queue flush를 명시적으로 마지막 한 쌍을 남겨둬서 조작이 drop되는 일이 없도록 함
        - temperature unit 변경시 refresh가 빨라 오히려 UX가 나빠짐. 화면 조작이 가능할 때 refresh되도록 변경
- ~~automode 00:00으로 시작하면 08:00으로 강제로 정규화하는 문제~~
    - [NOTE] : automode start cb 내부에 강제로 정규화하는 코드가 있음. 그 코드를 주석처리해서 해결했는데, 뭔가 지우면 버그가 생긴다는 주석이 있음.